[pytest]
# Tests import the package absolutely (`from airllm import ...`), so pytest is run from this
# directory (air_llm/), which is also where the importable `airllm` package lives.
testpaths = tests
